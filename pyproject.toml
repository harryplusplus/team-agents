[project]
name = "team-agents"
version = "0.1.0"
description = "Task Multi-Agent System"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langgraph>=1.0.7",
    "langgraph-checkpoint-postgres>=3.0.4",
    "nanoid>=2.0.0",
    "python-dotenv>=1.2.1",
]
