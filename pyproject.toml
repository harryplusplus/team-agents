[project]
name = "team-agents"
version = "0.1.0"
description = "Task Multi-Agent System"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langgraph>=1.0.7",
    "python-dotenv>=1.2.1",
]
